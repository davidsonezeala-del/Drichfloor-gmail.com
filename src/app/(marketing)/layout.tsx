// This is a placeholder file for the marketing layout
// The actual layout is handled by the root layout.tsx

export default function MarketingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
